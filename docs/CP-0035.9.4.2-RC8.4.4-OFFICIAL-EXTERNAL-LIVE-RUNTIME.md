# CP-0035.9.4.2 RC8.4.4 â€” Official External Live Runtime

Status: IMPLEMENTED

## Production chain

`CAMERA FRAME`
â†’ `CAMERA POSE GUARD`
â†’ `CAMERA OWNERSHIP`
â†’ `RECEIVING SIDE`
â†’ `OFFICIAL EXTERNAL GRID v2`
â†’ `PILLOW / BALL COLOR`
â†’ `Z0 CANDIDATE`
â†’ `UP AFTER THE FACT`
â†’ `OUT`
â†’ `SOFT FLOOR GLOW`

## Safety

- BLOCKED pose: no Pillow scan, no cell loop, no Z0, no OUT, no glow.
- MICRO_ADJUST: repair image geometry/reference, clear event state, suppress the transition frame, then require a later SAFE frame.
- No ownership for the current receiving side: scan zero cells.
- Candidate alone: never OUT.
- Only a current camera-owned `OUT_*` cell may become an `OfficialOutCall`.
- No interior-grid fallback exists.

## Architecture cleanup

This runtime uses the OFFICIAL `external_grid_frame_loop.py` v2 calibration model.
The earlier experimental `external_grid_z0_game.py` remains only for historical tests/demos and is not used by production runtime.