# CP-0023.3 — AI Assisted Labeling Studio

Flow:
- AUTO suggestion appears
- SPACE = accept + next
- click = correct
- N = no ball
- AUTO ASSIST can be toggled on/off

The assistant uses HSV full-frame candidate scanning, temporal proximity to recent positive labels, and Smart Bounding Box for click correction.

It never overwrites an existing annotation automatically.
