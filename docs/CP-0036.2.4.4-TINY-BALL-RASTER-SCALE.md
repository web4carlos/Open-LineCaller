# CP-0036.2.4.4 — Tiny-Ball Raster Scale + Pre-Z0 Diagnostics

The real 640x360 FAR_LEFT reference contact at video frame 3677 maps to
OUT_LEFT cell 1397 with 2.168 BU outside clearance. Geometry passes.

The local projected pickleball diameter is only 1.654 px. The historical
max scale ratio 1.80 permits 2.978 px, so an ordinary 3 px raster component can
be rejected by about 0.022 px even though the image cannot represent fractional
connected-component width.

For expected projected diameters below 3 px only, this pack applies +/-0.5 px
raster quantization allowance. Expected diameters >= 3 px keep the historical
proportional gate unchanged.

The same tiny-ball rule is used by raw external Z0, projected Z0 signature
matching, and localized contact appearance recovery.

Safety remains unchanged: OUTSIDE GRID ONLY, selected-ball identity, projected
anchor matching, motion memory, line-touch safety, UP confirmation, camera
ownership, pose safety, and official OUT event requirements all remain.

New telemetry:
- EXT CELL HITS
- RAW Z0 TOTAL
- SCALE LOW
- SCALE HIGH
- RASTER ACCEPTS
- SCALE REASON
- SCALE CELL
- OBSERVED PX
- EXPECTED PX
- SCALE RATIO
