# CP-0019.2 Technical Design

Goal: accelerate creation of ground truth from real match videos.

Components:
- AnnotationCandidate
- AnnotationLabel
- CandidateScanner
- AnnotationSession
- SmartAnnotationStudioWindow
- zoom helper

The existing pipeline is used only to locate candidate events. The human operator
must confirm IN / OUT / SKIP.
