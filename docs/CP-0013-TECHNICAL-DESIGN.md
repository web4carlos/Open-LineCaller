# CP-0013 Technical Design

DatasetStudioWindow handles video navigation and save/load.
AnnotationVideoWidget maps mouse drags safely back to source-frame coordinates.
DatasetStudioSession is the editing layer over the canonical CP-0012 models.

Save runs validate_clip(); ERROR issues block save.
