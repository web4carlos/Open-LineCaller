# CP-0035.9.4.2 RC8.3.2 — One Ball / One Z0 Cell

Status: EXPERIMENTAL / NOT COMMITTED

## Locked visual/runtime rule

This gate shows at most ONE Z0 candidate cell per frame.

It does not show:
- earlier ball positions;
- later ball positions;
- path;
- trajectory;
- history;
- multiple candidate cells.

Runtime:

`FRAME -> PILLOW(calibrated reference) -> SPECIFIC EXTERNAL ROI -> ONE Z0 CANDIDATE`

The Z0 cell is only a candidate in this RC.

The next gate may perform the already-agreed after-the-fact confirmation:

`ONE Z0 CANDIDATE -> SAME BALL ABOVE -> BINGO`

No DOWN requirement.
No tracker.
No XYZ.
No path.
